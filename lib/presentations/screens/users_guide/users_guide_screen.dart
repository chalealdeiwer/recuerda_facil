import 'package:flutter/material.dart';

class UsersGuideScreen extends StatelessWidget {
  const UsersGuideScreen({super.key});
static const name = 'users_guide_screen';
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Users guide Screen"),
      ),
    );
  }
}